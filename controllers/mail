// package controllers

// import (
// 	"crypto/hmac"
// 	"crypto/sha1"
// 	"encoding/base64"
// )

// func Single(searct byte, data string) string {
// 	key := byte(searct)
// 	mac := hmac.New(sha1.New(), key)
// 	mac.Write([]byte(data))
// 	s := base64.StdEncoding.EncodeToString(mac.Sum(nil))
// 	return s
// }
